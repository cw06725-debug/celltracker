# CellTracker v0.3.5

Fixes Recording Detail map layout clipping and adds visible start/end GPS markers so stationary recordings still show a location point.

# CellTracker v0.3.4

Hotfix for v0.3.3 build failure: enables BuildConfig generation so VERSION_NAME references compile. Keeps OSM User-Agent and map jitter fixes from v0.3.3.
