# CellTracker v0.5.2

- Export now creates CSV + responsive HTML Summary + XLSX report.
- XLSX contains `Summary` and `Raw Data` sheets.
- Fixed narrow-screen HTML summary header overlap.
- Export success actions: Open Summary / Open Excel / Share / Close.
- Removed unreliable Open Folder action.
